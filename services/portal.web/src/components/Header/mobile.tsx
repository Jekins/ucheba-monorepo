import { FC } from 'react'
import styled from 'styled-components'
import DefaultHeader from './_default'
import { THeaderProps } from './_types'

const Header = styled(DefaultHeader)`` as FC<THeaderProps>

export default Header
